import {styled} from "@mui/material/styles";
import {Avatar} from "@mui/material";

const StyledAvatar = styled(Avatar)(({width, height }) => ({
    width,
    height,
}))
export default StyledAvatar;